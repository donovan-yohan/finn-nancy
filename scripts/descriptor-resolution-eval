#!/usr/bin/env bash
set -euo pipefail

REPO="$(cd "$(dirname "$0")/.." && pwd)"

exec uv run --project "$REPO" --frozen python \
  -m app.evals.fn149b_descriptor_precision \
  --packet-dir "$REPO/tests/fixtures/fn149b" \
  "$@"
