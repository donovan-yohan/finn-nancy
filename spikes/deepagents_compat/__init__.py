"""Mistral Deep Agents compatibility spike.

The code in this package is intentionally isolated from the production app.
Run it manually with:

    uv run --group spike python -m spikes.deepagents_compat.run
"""
